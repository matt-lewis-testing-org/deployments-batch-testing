name: Output Scheduled Actions
description: ""

inputs:
  token:
    description: The GitHub token used to create an authenticated client
    default: ${{ github.token }}
    required: false

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@3

    - uses: actions/github-script@7
      env:
        GITHUB_ACTION_PATH: ${{ github.action_path }}
      with:
        script: |
          const { GITHUB_ACTION_PATH } = process.env;
          const runScript = require(`${GITHUB_ACTION_PATH}/output-action.js`);

          const scriptInputs = {
            token: "${{ inputs.token }}",
            repo: "${{ github.repository }}"
          };

          await runScript({ github, context, core }, scriptInputs);

